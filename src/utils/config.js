import { AUTH_URL, AUTH_API_TOKEN_URL, CLIENT_ID, CLIENT_SECRET_ID, REDIRECT_URI, REDIRECT_URI_ANDROID } from 'react-native-dotenv';

export default Credentials = {
    authUrl: AUTH_URL,
    authTokenUrl: AUTH_API_TOKEN_URL,
    clientId: CLIENT_ID,
    clientSecret: CLIENT_SECRET_ID,
    redirectUri: REDIRECT_URI,
    redirectUri_android: REDIRECT_URI_ANDROID,
}

